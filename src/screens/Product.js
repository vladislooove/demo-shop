import React from 'react';
import Layout from '../components/Layout';

export default class Product extends React.Component {
    render() {
        return (
            <Layout>
                <p>Look, ya, im Product page</p>
            </Layout>
        );
    }
}